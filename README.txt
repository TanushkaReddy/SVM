SVM-Support Vector Machnine
1.Complex data seperation
2.Classification problem
3.Binary Classification
Examples:
      Spam/not Spam
      Dog/Cat
      Disease/no Disease
Logistic regression works well when data is linearly seperable,but what if
     1.data is complex
     2.classes overlapped
     3.decision boundary is not straight
  That is where SVM becomes powerful


1. Linear = linear data
2. poly  = polynominal relationship
3. rbf = non-linear complex data
4. sigmoid = neural-network-like behavior

c=1    
controls overfitting vs underfitting     
small c ---> wider margin--->underfit
large c --->tries to classify all points-->may overfit